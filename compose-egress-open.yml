# This compose file overrides the network-limiting settings of `compose.yml`,
# while retaining all the other settings; for use with
# `run-claude-egress-open.sh`
services:
  claude:
    # No proxy in this mode: unset the vars `compose.yml` sets on the base
    # service so egress is allowed
    environment:
      HTTPS_PROXY: ""
      HTTP_PROXY: ""
      NO_PROXY: ""

networks:
  claude-net:
    # Allow networking
    internal: false
